package com.nolevelcap.main;

public enum State {
	MAIN_MENU_STATE(0),
	PLAY_GAME_STATE = 1,
	POWERS_MENU_STATE = 2;
	
	 private final int mass;   // in kilograms
	 State(int mass) {
	        this.mass = mass;
	        this.radius = radius;
	    }
}

package com.nolevelcap.main;

import java.awt.Rectangle;

import org.lwjgl.input.Mouse;

public class MouseHandelling {
	
	private x, y;
	
	public void logic(){
		int x = Mouse.getX(); // will return the X coordinate on the Display.
		int y = Mouse.getY(); // will return the Y coordinate on the Display.
	}
	
	public boolean doesTheMouseIntersectThis(Rectangle comp){
		Rectangle moundRect = new Rectangle();
		
		
		return null;
	}
}
